{
  "name": "parallels",
  "version": "1.0.0",
  "private": true,
  "description": "PARALLELS — a multiverse game with an AI engine that writes your parallel lives.",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
